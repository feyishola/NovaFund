// backend/src/utils/s3.ts

export async function saveToS3(path: string, content: string) {
    console.log(`Pretend saving ${path} to S3`);
}

export async function deleteFromS3(path: string) {
    console.log(`Pretend deleting ${path} from S3`);
}